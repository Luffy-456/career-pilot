name: CareerPilot CI

on:
  push:
    branches: [ "feature/ci-pipeline" ]
  pull_request:
    branches: [ "main" ]

jobs:
  backend-ci:
    name: Backend Build & Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18.x'
          cache: 'npm'
          cache-dependency-path: ./backend/package-lock.json

      - name: Install dependencies
        # FIXED: Added --legacy-peer-deps to resolve the Cloudinary/Multer conflict
        run: npm ci --legacy-peer-deps

      - name: Run Linter
        run: npm run lint

      - name: Run Tests
        run: npm test

  frontend-ci:
    name: Frontend Build & Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18.x'
          cache: 'npm'
          # FIXED: Removed cache-dependency-path since the lockfile is missing

      - name: Install dependencies
        # FIXED: Swapped 'npm ci' to 'npm install' to bypass the missing lockfile
        run: npm install

      - name: Run Linter
        run: npm run lint

      # Vitest requires the 'run' flag in CI environments to prevent hanging in watch mode
      - name: Run Tests
        run: npx vitest run

      - name: Build Project
        run: npm run build
